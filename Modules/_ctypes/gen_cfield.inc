/* Generated by Modules/_ctypes/gen_cfield.py */
static PyObject *
u8_set(void *ptr, PyObject *value, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 1));
    uint8_t val;
    if (PyLong_Check(value)
            && PyUnstable_Long_IsCompact((PyLongObject *)value)) {
        val = (uint8_t)PyUnstable_Long_CompactValue(
            (PyLongObject *)value);
    }
    else {
        int res = PyLong_AsNativeBytes(
            value, &val, 1, Py_ASNATIVEBYTES_DEFAULTS);
        if (res < 0) {
            return NULL;
        }
    }
    *(uint8_t*)ptr = SET(uint8_t, *(uint8_t*)ptr, val, size);
    _RET(value);
}

static PyObject *
u8_get(void *ptr, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 1));
    uint8_t val;
    memcpy(&val, ptr, sizeof(val));
    GET_BITFIELD(val, size);
    return PyLong_FromUInt32(val);
}

static PyObject *
s8_set(void *ptr, PyObject *value, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 1));
    int8_t val;
    if (PyLong_Check(value)
            && PyUnstable_Long_IsCompact((PyLongObject *)value)) {
        val = (int8_t)PyUnstable_Long_CompactValue(
            (PyLongObject *)value);
    }
    else {
        int res = PyLong_AsNativeBytes(
            value, &val, 1, Py_ASNATIVEBYTES_DEFAULTS);
        if (res < 0) {
            return NULL;
        }
    }
    *(int8_t*)ptr = SET(int8_t, *(int8_t*)ptr, val, size);
    _RET(value);
}

static PyObject *
s8_get(void *ptr, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 1));
    int8_t val;
    memcpy(&val, ptr, sizeof(val));
    GET_BITFIELD(val, size);
    return PyLong_FromInt32(val);
}

static PyObject *
u16_set(void *ptr, PyObject *value, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 2));
    uint16_t val;
    if (PyLong_Check(value)
            && PyUnstable_Long_IsCompact((PyLongObject *)value)) {
        val = (uint16_t)PyUnstable_Long_CompactValue(
            (PyLongObject *)value);
    }
    else {
        int res = PyLong_AsNativeBytes(
            value, &val, 2, Py_ASNATIVEBYTES_DEFAULTS);
        if (res < 0) {
            return NULL;
        }
    }
    *(uint16_t*)ptr = SET(uint16_t, *(uint16_t*)ptr, val, size);
    _RET(value);
}

static PyObject *
u16_get(void *ptr, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 2));
    uint16_t val;
    memcpy(&val, ptr, sizeof(val));
    GET_BITFIELD(val, size);
    return PyLong_FromUInt32(val);
}

static PyObject *
u16_set_sw(void *ptr, PyObject *value, Py_ssize_t size)
{
    uint16_t val;
    PyObject *res = u16_set(&val, value, size);
    if (res == NULL) {
        return NULL;
    }
    uint16_t field;
    memcpy(&field, ptr, sizeof(field));
    field = _Py_bswap16(field);
    field = SET(uint16_t, field, val, size);
    field = _Py_bswap16(field);
    memcpy(ptr, &field, sizeof(field));
    _RET(value);
}

static PyObject *
u16_get_sw(void *ptr, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 2));
    uint16_t val;
    memcpy(&val, ptr, sizeof(val));
    val = _Py_bswap16(val);
    GET_BITFIELD(val, size);
    return PyLong_FromUInt32(val);
}

static PyObject *
s16_set(void *ptr, PyObject *value, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 2));
    int16_t val;
    if (PyLong_Check(value)
            && PyUnstable_Long_IsCompact((PyLongObject *)value)) {
        val = (int16_t)PyUnstable_Long_CompactValue(
            (PyLongObject *)value);
    }
    else {
        int res = PyLong_AsNativeBytes(
            value, &val, 2, Py_ASNATIVEBYTES_DEFAULTS);
        if (res < 0) {
            return NULL;
        }
    }
    *(int16_t*)ptr = SET(int16_t, *(int16_t*)ptr, val, size);
    _RET(value);
}

static PyObject *
s16_get(void *ptr, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 2));
    int16_t val;
    memcpy(&val, ptr, sizeof(val));
    GET_BITFIELD(val, size);
    return PyLong_FromInt32(val);
}

static PyObject *
s16_set_sw(void *ptr, PyObject *value, Py_ssize_t size)
{
    int16_t val;
    PyObject *res = s16_set(&val, value, size);
    if (res == NULL) {
        return NULL;
    }
    int16_t field;
    memcpy(&field, ptr, sizeof(field));
    field = _Py_bswap16(field);
    field = SET(int16_t, field, val, size);
    field = _Py_bswap16(field);
    memcpy(ptr, &field, sizeof(field));
    _RET(value);
}

static PyObject *
s16_get_sw(void *ptr, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 2));
    int16_t val;
    memcpy(&val, ptr, sizeof(val));
    val = _Py_bswap16(val);
    GET_BITFIELD(val, size);
    return PyLong_FromInt32(val);
}

static PyObject *
u32_set(void *ptr, PyObject *value, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 4));
    uint32_t val;
    if (PyLong_Check(value)
            && PyUnstable_Long_IsCompact((PyLongObject *)value)) {
        val = (uint32_t)PyUnstable_Long_CompactValue(
            (PyLongObject *)value);
    }
    else {
        int res = PyLong_AsNativeBytes(
            value, &val, 4, Py_ASNATIVEBYTES_DEFAULTS);
        if (res < 0) {
            return NULL;
        }
    }
    *(uint32_t*)ptr = SET(uint32_t, *(uint32_t*)ptr, val, size);
    _RET(value);
}

static PyObject *
u32_get(void *ptr, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 4));
    uint32_t val;
    memcpy(&val, ptr, sizeof(val));
    GET_BITFIELD(val, size);
    return PyLong_FromUInt32(val);
}

static PyObject *
u32_set_sw(void *ptr, PyObject *value, Py_ssize_t size)
{
    uint32_t val;
    PyObject *res = u32_set(&val, value, size);
    if (res == NULL) {
        return NULL;
    }
    uint32_t field;
    memcpy(&field, ptr, sizeof(field));
    field = _Py_bswap32(field);
    field = SET(uint32_t, field, val, size);
    field = _Py_bswap32(field);
    memcpy(ptr, &field, sizeof(field));
    _RET(value);
}

static PyObject *
u32_get_sw(void *ptr, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 4));
    uint32_t val;
    memcpy(&val, ptr, sizeof(val));
    val = _Py_bswap32(val);
    GET_BITFIELD(val, size);
    return PyLong_FromUInt32(val);
}

static PyObject *
s32_set(void *ptr, PyObject *value, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 4));
    int32_t val;
    if (PyLong_Check(value)
            && PyUnstable_Long_IsCompact((PyLongObject *)value)) {
        val = (int32_t)PyUnstable_Long_CompactValue(
            (PyLongObject *)value);
    }
    else {
        int res = PyLong_AsNativeBytes(
            value, &val, 4, Py_ASNATIVEBYTES_DEFAULTS);
        if (res < 0) {
            return NULL;
        }
    }
    *(int32_t*)ptr = SET(int32_t, *(int32_t*)ptr, val, size);
    _RET(value);
}

static PyObject *
s32_get(void *ptr, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 4));
    int32_t val;
    memcpy(&val, ptr, sizeof(val));
    GET_BITFIELD(val, size);
    return PyLong_FromInt32(val);
}

static PyObject *
s32_set_sw(void *ptr, PyObject *value, Py_ssize_t size)
{
    int32_t val;
    PyObject *res = s32_set(&val, value, size);
    if (res == NULL) {
        return NULL;
    }
    int32_t field;
    memcpy(&field, ptr, sizeof(field));
    field = _Py_bswap32(field);
    field = SET(int32_t, field, val, size);
    field = _Py_bswap32(field);
    memcpy(ptr, &field, sizeof(field));
    _RET(value);
}

static PyObject *
s32_get_sw(void *ptr, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 4));
    int32_t val;
    memcpy(&val, ptr, sizeof(val));
    val = _Py_bswap32(val);
    GET_BITFIELD(val, size);
    return PyLong_FromInt32(val);
}

static PyObject *
u64_set(void *ptr, PyObject *value, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 8));
    uint64_t val;
    if (PyLong_Check(value)
            && PyUnstable_Long_IsCompact((PyLongObject *)value)) {
        val = (uint64_t)PyUnstable_Long_CompactValue(
            (PyLongObject *)value);
    }
    else {
        int res = PyLong_AsNativeBytes(
            value, &val, 8, Py_ASNATIVEBYTES_DEFAULTS);
        if (res < 0) {
            return NULL;
        }
    }
    *(uint64_t*)ptr = SET(uint64_t, *(uint64_t*)ptr, val, size);
    _RET(value);
}

static PyObject *
u64_get(void *ptr, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 8));
    uint64_t val;
    memcpy(&val, ptr, sizeof(val));
    GET_BITFIELD(val, size);
    return PyLong_FromUInt64(val);
}

static PyObject *
u64_set_sw(void *ptr, PyObject *value, Py_ssize_t size)
{
    uint64_t val;
    PyObject *res = u64_set(&val, value, size);
    if (res == NULL) {
        return NULL;
    }
    uint64_t field;
    memcpy(&field, ptr, sizeof(field));
    field = _Py_bswap64(field);
    field = SET(uint64_t, field, val, size);
    field = _Py_bswap64(field);
    memcpy(ptr, &field, sizeof(field));
    _RET(value);
}

static PyObject *
u64_get_sw(void *ptr, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 8));
    uint64_t val;
    memcpy(&val, ptr, sizeof(val));
    val = _Py_bswap64(val);
    GET_BITFIELD(val, size);
    return PyLong_FromUInt64(val);
}

static PyObject *
s64_set(void *ptr, PyObject *value, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 8));
    int64_t val;
    if (PyLong_Check(value)
            && PyUnstable_Long_IsCompact((PyLongObject *)value)) {
        val = (int64_t)PyUnstable_Long_CompactValue(
            (PyLongObject *)value);
    }
    else {
        int res = PyLong_AsNativeBytes(
            value, &val, 8, Py_ASNATIVEBYTES_DEFAULTS);
        if (res < 0) {
            return NULL;
        }
    }
    *(int64_t*)ptr = SET(int64_t, *(int64_t*)ptr, val, size);
    _RET(value);
}

static PyObject *
s64_get(void *ptr, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 8));
    int64_t val;
    memcpy(&val, ptr, sizeof(val));
    GET_BITFIELD(val, size);
    return PyLong_FromInt64(val);
}

static PyObject *
s64_set_sw(void *ptr, PyObject *value, Py_ssize_t size)
{
    int64_t val;
    PyObject *res = s64_set(&val, value, size);
    if (res == NULL) {
        return NULL;
    }
    int64_t field;
    memcpy(&field, ptr, sizeof(field));
    field = _Py_bswap64(field);
    field = SET(int64_t, field, val, size);
    field = _Py_bswap64(field);
    memcpy(ptr, &field, sizeof(field));
    _RET(value);
}

static PyObject *
s64_get_sw(void *ptr, Py_ssize_t size)
{
    assert(NUM_BITS(size) || (size == 8));
    int64_t val;
    memcpy(&val, ptr, sizeof(val));
    val = _Py_bswap64(val);
    GET_BITFIELD(val, size);
    return PyLong_FromInt64(val);
}

struct formattable {
    struct fielddesc fmt_s;  // char *
    struct fielddesc fmt_b;  // signed char
    struct fielddesc fmt_B;  // unsigned char
    struct fielddesc fmt_c;  // char
    struct fielddesc fmt_d;  // double
#if defined(Py_HAVE_C_COMPLEX) && defined(Py_FFI_SUPPORT_C_COMPLEX)
    struct fielddesc fmt_C;  // double complex
    struct fielddesc fmt_E;  // float complex
    struct fielddesc fmt_F;  // long double complex
#endif
    struct fielddesc fmt_g;  // long double
    struct fielddesc fmt_f;  // float
    struct fielddesc fmt_h;  // short
    struct fielddesc fmt_H;  // unsigned short
    struct fielddesc fmt_i;  // int
    struct fielddesc fmt_I;  // unsigned int
    struct fielddesc fmt_l;  // long
    struct fielddesc fmt_L;  // unsigned long
    struct fielddesc fmt_q;  // long long
    struct fielddesc fmt_Q;  // unsigned long long
    struct fielddesc fmt_P;  // void *
    struct fielddesc fmt_z;  // char *
    struct fielddesc fmt_u;  // wchar_t
    struct fielddesc fmt_U;  // wchar_t *
    struct fielddesc fmt_Z;  // wchar_t *
#if defined(MS_WIN32)
    struct fielddesc fmt_X;  // BSTR
#endif
    struct fielddesc fmt_v;  // vBOOL
    struct fielddesc fmt_bool;  // bool
    struct fielddesc fmt_O;  // PyObject *
};

static struct formattable formattable = {
    .fmt_s = {'s', s_set, s_get, NULL},
    .fmt_b = {'b', b_set, b_get, NULL},
    .fmt_B = {'B', B_set, B_get, NULL},
    .fmt_c = {'c', c_set, c_get, NULL},
    .fmt_d = {'d', d_set, d_get, NULL, d_set_sw, d_get_sw},
#if defined(Py_HAVE_C_COMPLEX) && defined(Py_FFI_SUPPORT_C_COMPLEX)
    .fmt_C = {'C', C_set, C_get, NULL},
    .fmt_E = {'E', E_set, E_get, NULL},
    .fmt_F = {'F', F_set, F_get, NULL},
#endif
    .fmt_g = {'g', g_set, g_get, NULL},
    .fmt_f = {'f', f_set, f_get, NULL, f_set_sw, f_get_sw},
    .fmt_h = {'h', h_set, h_get, NULL, h_set_sw, h_get_sw},
    .fmt_H = {'H', H_set, H_get, NULL, H_set_sw, H_get_sw},
    .fmt_i = {'i', i_set, i_get, NULL, i_set_sw, i_get_sw},
    .fmt_I = {'I', I_set, I_get, NULL, I_set_sw, I_get_sw},
    .fmt_l = {'l', l_set, l_get, NULL, l_set_sw, l_get_sw},
    .fmt_L = {'L', L_set, L_get, NULL, L_set_sw, L_get_sw},
    .fmt_q = {'q', q_set, q_get, NULL, q_set_sw, q_get_sw},
    .fmt_Q = {'Q', Q_set, Q_get, NULL, Q_set_sw, Q_get_sw},
    .fmt_P = {'P', P_set, P_get, NULL},
    .fmt_z = {'z', z_set, z_get, NULL},
    .fmt_u = {'u', u_set, u_get, NULL},
    .fmt_U = {'U', U_set, U_get, NULL},
    .fmt_Z = {'Z', Z_set, Z_get, NULL},
#if defined(MS_WIN32)
    .fmt_X = {'X', X_set, X_get, NULL},
#endif
    .fmt_v = {'v', v_set, v_get, NULL},
    .fmt_bool = {'?', bool_set, bool_get, NULL, bool_set_sw, bool_get_sw},
    .fmt_O = {'O', O_set, O_get, NULL},
};
struct fielddesc *
_ctypes_get_fielddesc(const char *fmt)
{
    static int initialized = 0;
    if (!initialized) {
        /* Delayed initialization. Windows cannot statically reference dynamically
           loaded addresses from DLLs. */
        formattable.fmt_s.pffi_type = &ffi_type_pointer;
        formattable.fmt_b.pffi_type = &ffi_type_schar;
        formattable.fmt_B.pffi_type = &ffi_type_uchar;
        formattable.fmt_c.pffi_type = &ffi_type_schar;
        formattable.fmt_d.pffi_type = &ffi_type_double;
#if defined(Py_HAVE_C_COMPLEX) && defined(Py_FFI_SUPPORT_C_COMPLEX)
        formattable.fmt_C.pffi_type = &ffi_type_complex_double;
        formattable.fmt_E.pffi_type = &ffi_type_complex_float;
        formattable.fmt_F.pffi_type = &ffi_type_complex_longdouble;
#endif
        formattable.fmt_g.pffi_type = &ffi_type_longdouble;
        formattable.fmt_f.pffi_type = &ffi_type_float;
        formattable.fmt_h.pffi_type = &ffi_type_sshort;
        formattable.fmt_H.pffi_type = &ffi_type_ushort;
        formattable.fmt_i.pffi_type = &ffi_type_sint;
        formattable.fmt_I.pffi_type = &ffi_type_uint;
        formattable.fmt_l.pffi_type = &ctypes_ffi_type_slong;
        formattable.fmt_L.pffi_type = &ctypes_ffi_type_ulong;
        formattable.fmt_q.pffi_type = &ctypes_ffi_type_slonglong;
        formattable.fmt_Q.pffi_type = &ctypes_ffi_type_ulonglong;
        formattable.fmt_P.pffi_type = &ffi_type_pointer;
        formattable.fmt_z.pffi_type = &ffi_type_pointer;
        formattable.fmt_u.pffi_type = &ctypes_ffi_type_wchar_t;
        formattable.fmt_U.pffi_type = &ffi_type_pointer;
        formattable.fmt_Z.pffi_type = &ffi_type_pointer;
#if defined(MS_WIN32)
        formattable.fmt_X.pffi_type = &ffi_type_pointer;
#endif
        formattable.fmt_v.pffi_type = &ffi_type_sshort;
        formattable.fmt_bool.pffi_type = &ctypes_ffi_type_bool;
        formattable.fmt_O.pffi_type = &ffi_type_pointer;
        initialized = 1;
    }
    switch(fmt[0]) {
        case 's': return &formattable.fmt_s;
        case 'b': return &formattable.fmt_b;
        case 'B': return &formattable.fmt_B;
        case 'c': return &formattable.fmt_c;
        case 'd': return &formattable.fmt_d;
#if defined(Py_HAVE_C_COMPLEX) && defined(Py_FFI_SUPPORT_C_COMPLEX)
        case 'C': return &formattable.fmt_C;
        case 'E': return &formattable.fmt_E;
        case 'F': return &formattable.fmt_F;
#endif
        case 'g': return &formattable.fmt_g;
        case 'f': return &formattable.fmt_f;
        case 'h': return &formattable.fmt_h;
        case 'H': return &formattable.fmt_H;
        case 'i': return &formattable.fmt_i;
        case 'I': return &formattable.fmt_I;
        case 'l': return &formattable.fmt_l;
        case 'L': return &formattable.fmt_L;
        case 'q': return &formattable.fmt_q;
        case 'Q': return &formattable.fmt_Q;
        case 'P': return &formattable.fmt_P;
        case 'z': return &formattable.fmt_z;
        case 'u': return &formattable.fmt_u;
        case 'U': return &formattable.fmt_U;
        case 'Z': return &formattable.fmt_Z;
#if defined(MS_WIN32)
        case 'X': return &formattable.fmt_X;
#endif
        case 'v': return &formattable.fmt_v;
        case '?': return &formattable.fmt_bool;
        case 'O': return &formattable.fmt_O;
    }
    return NULL;
}
